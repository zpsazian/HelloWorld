# HelloWorld (macOS)

A minimal macOS Swift command-line application that prints `Hello, World!`.

## Requirements

- macOS 13 or newer
- Xcode or Swift toolchain with Swift Package Manager

## Build and run

```sh
swift run HelloWorld
```
